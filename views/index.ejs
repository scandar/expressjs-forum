<%include partials/header %>

<h1>index page</h1>

<h2>recent posts</h2>
<% posts.forEach(function(post) { %>
    <h3><%=post.title%></h3>
    <% if(post.image){ %> <img src="<%=post.image%>" alt="<%= post.title %>"> <%}%>
    <p><%=post.content%></p>
    <a href="/index/<%=post._id%>">Read more</a>
<%});%>

<%include partials/footer %>